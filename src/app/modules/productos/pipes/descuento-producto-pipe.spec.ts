import { DescuentoProductoPipe } from './descuento-producto-pipe';

describe('DescuentoProductoPipe', () => {
  it('create an instance', () => {
    const pipe = new DescuentoProductoPipe();
    expect(pipe).toBeTruthy();
  });
});
